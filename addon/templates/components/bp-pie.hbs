{{echarts-chart classNames='bp-chart'
                elementId=eid
                option=result
                onChartReady=(action onChartReady)
                opts=opts
                onEvents=onEvents}}
